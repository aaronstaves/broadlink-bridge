{
  "name": "broadlink-bridge",
  "version": "0.0.1",
  "description": "Broadlink RMPro2 IR/RF devices accessible via endpoints",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "broadlinkjs": "^0.1.0",
    "express": "^4.15.2",
    "mkdirp": "^0.5.1"
  }
}
